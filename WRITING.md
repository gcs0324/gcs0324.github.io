# WRITING.md

内容写作规范，适用于本项目所有 `doc/*.html` 知识文章。

---

## 受众定位

目标读者是**使用 Kafka / Redis 的 Java 开发者**，不是搞中间件源码的人。解释要能与日常开发经验挂钩，不需要覆盖所有源码级细节。

---

## 概念解释

**每个核心概念单独展开，不要堆在一段里。**

每个概念至少给出：定义（是什么）→ 作用（用来干什么）→ 关键特性或约束。有合适的比喻时加上，没有就不加。

```
✗ 差：Topic 是逻辑概念，Partition 是物理存储单元，Replica 分为 Leader 和 Follower，Broker 是物理节点。

✓ 好：
  Broker（服务器节点）：Kafka 集群中的一台服务器就是一个 Broker，负责接收消息、存储到磁盘、响应 Consumer 拉取请求。
  Topic（主题）：消息的逻辑分类，类似数据库中的一张表。Topic 本身不存数据，数据存在它的 Partition 里。
  Partition（分区）：...
```

---

## 定义语言

**用口语和具体数字解释抽象概念，不要只照搬官方文档原话。**

```
✗ 差：LEO（Log End Offset）：该 Replica 下一条消息的 offset。

✓ 好：LEO 是该 Replica 下一条消息的 offset（副本日志中下一条消息即将被写入的位置，即当前最后一条消息的 offset + 1。
       比如副本已存了 offset 0～99 共 100 条消息，LEO 就是 100）
```

---

## 标题措辞

**标题直接说主题，不加"先记住："之类的引导前缀。**

```
✗ 差：先记住：Controller 是什么，做什么
✓ 好：Controller 是什么，做什么
```

---

## 术语最小化

**只用必要的术语，不引入多余的专业词汇。**

能用"Controller"说清楚的，不说"Active Controller"。能用"节点"说清楚的，不说"Quorum Member"。新术语出现时必须紧跟解释，不能只扔一个词。

---

## 步骤编号

**编号只给流程中真实的执行步骤，配套信息不占编号。**

判断标准：这一条是"系统/组件实际执行的动作"吗？是则编号，否则用提示框。

```
流程结束点示例（Kafka Partition Leader 故障转移）：
  ① Follower 脱离 ISR
  ② Controller 检测 Broker 下线
  ③ Controller 从 ISR 选新 Leader   ← 流程到此结束

以下内容不是流程步骤，不应编号：
  - ISR 耗尽时的 unclean election 配置取舍  → .note.warn 提示框
  - Producer 端的重试参数配置               → .note.info 提示框
```

---

## 教程例题必须解释“为什么”

**例题不能只罗列操作和答案。每一步都要说明这一步的依据，以及它如何导向下一步。**

读者第一次接触概念时，应当能只看页面独立复现解法。禁止出现以下推理跳步：

- 直接写“检查这些位置”，但不解释位置如何确定。
- 直接写“相除得到余数”，但不展示关键异或或长除过程。
- 直接套公式，但不解释分子、分母分别对应哪段时间或哪类数量。
- 直接给出校验结果、状态变化或设备动作，但不解释判定规则。

```
✗ 差：
  P1 检查位置 1、3、5、7。
  重新校验得到 101，所以第 5 位错误。

✓ 好：
  位置编号 1、3、5、7 的二进制最低位都是 1，所以它们属于 P1 组。
  接收端重新异或 P1 组得到 S1=1，P2 组得到 S2=0，P4 组得到 S4=1。
  按高位到低位组合为 S4S2S1=101₂=5，因此第 5 位错误。
```

每个计算型核心概念至少包含：

1. **规则来源**：公式或分组为什么成立。
2. **完整例题**：使用具体数字，不只写符号。
3. **逐步解法**：关键中间值不得省略。
4. **结果检查**：单位、范围、边界或反向验证。
5. **常见误区**：只写最容易导致错误的一两项。

---

## 例题深度服从学习优先级

备考文章必须先确定目标分数与 A/B/C 优先级，再决定例题深度，不能为了“教程完整”平均用力。

| 优先级 | 正文与例题要求 |
|------|------|
| A 必学 | 解释原理，给完整教程例题和考试型练习，步骤不得跳跃 |
| B 会辨析 | 给一个短场景说明如何应用；只有历年确有计算考法时才展开计算 |
| C 时间紧可跳过 | 说明定义和跳过理由，不添加长例题、字段推导或冷门计算 |

新增例题前必须回答：

1. 它是否服务于目标分数，而不是单纯展示知识完整性？
2. 它是否解决“知道结论但不会应用”的问题？
3. 它的讲解成本是否与可能分值、迁移价值相匹配？
4. 页面是否已经有能覆盖同一能力的例题？

若第 1 或第 3 项为否，应删除、压缩为短场景，或降到跳过提示。

---

## 教程文章交付前自检

逐节检查：

- 所有 A 类公式是否解释来源，并有可独立复现的例题。
- 每个例题是否存在“得到”“显然”“因此”后直接跳到陌生结论的情况。
- B 类是否只讲到会判断、会套用，没有扩展成低收益难题。
- C 类是否误配了长例题。
- 真题年份和题号是否有依据；没有依据时只能写“真题型例题”或“常见问法”。
- 页面标注的高频、中频、低频是否被写成虚假的精确命题概率。

---

## 提示框使用规则

| 情形 | 用法 |
|------|------|
| 刚描述的步骤存在边界情况或风险 | `.note.warn` 紧跟在该步骤内容下方 |
| 额外的参数配置、可选项、补充说明 | `.note.info` 独立放在卡片外部 |
| 面试技巧、记忆方法 | `.note.tip` |
| 推荐做法或最佳实践 | `.note.good` |

提示框不替代正文。正文说清楚主路径，提示框只补充边界或延伸信息。
