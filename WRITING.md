# WRITING.md

内容写作规范，适用于本项目所有 `doc/*.html` 知识文章。

---

## 受众定位

目标读者是**使用 Kafka / Redis 的 Java 开发者**，不是搞中间件源码的人。解释要能与日常开发经验挂钩，不需要覆盖所有源码级细节。

---

## 概念解释

**每个核心概念单独展开，不要堆在一段里。**

每个概念至少给出：定义（是什么）→ 作用（用来干什么）→ 关键特性或约束。有合适的比喻时加上，没有就不加。

```
✗ 差：Topic 是逻辑概念，Partition 是物理存储单元，Replica 分为 Leader 和 Follower，Broker 是物理节点。

✓ 好：
  Broker（服务器节点）：Kafka 集群中的一台服务器就是一个 Broker，负责接收消息、存储到磁盘、响应 Consumer 拉取请求。
  Topic（主题）：消息的逻辑分类，类似数据库中的一张表。Topic 本身不存数据，数据存在它的 Partition 里。
  Partition（分区）：...
```

---

## 定义语言

**用口语和具体数字解释抽象概念，不要只照搬官方文档原话。**

```
✗ 差：LEO（Log End Offset）：该 Replica 下一条消息的 offset。

✓ 好：LEO 是该 Replica 下一条消息的 offset（副本日志中下一条消息即将被写入的位置，即当前最后一条消息的 offset + 1。
       比如副本已存了 offset 0～99 共 100 条消息，LEO 就是 100）
```

---

## 标题措辞

**标题直接说主题，不加"先记住："之类的引导前缀。**

```
✗ 差：先记住：Controller 是什么，做什么
✓ 好：Controller 是什么，做什么
```

---

## 术语最小化

**只用必要的术语，不引入多余的专业词汇。**

能用"Controller"说清楚的，不说"Active Controller"。能用"节点"说清楚的，不说"Quorum Member"。新术语出现时必须紧跟解释，不能只扔一个词。

---

## 步骤编号

**编号只给流程中真实的执行步骤，配套信息不占编号。**

判断标准：这一条是"系统/组件实际执行的动作"吗？是则编号，否则用提示框。

```
流程结束点示例（Kafka Partition Leader 故障转移）：
  ① Follower 脱离 ISR
  ② Controller 检测 Broker 下线
  ③ Controller 从 ISR 选新 Leader   ← 流程到此结束

以下内容不是流程步骤，不应编号：
  - ISR 耗尽时的 unclean election 配置取舍  → .note.warn 提示框
  - Producer 端的重试参数配置               → .note.info 提示框
```

---

## 提示框使用规则

| 情形 | 用法 |
|------|------|
| 刚描述的步骤存在边界情况或风险 | `.note.warn` 紧跟在该步骤内容下方 |
| 额外的参数配置、可选项、补充说明 | `.note.info` 独立放在卡片外部 |
| 面试技巧、记忆方法 | `.note.tip` |
| 推荐做法或最佳实践 | `.note.good` |

提示框不替代正文。正文说清楚主路径，提示框只补充边界或延伸信息。
